# VITE_API_URL NÃO vai aqui: é embutida no bundle em tempo de build (ver o build arg
# no docker-compose.yml), não pode ser trocada por uma variável de runtime do container.
PORT=3000
NODE_ENV=production
